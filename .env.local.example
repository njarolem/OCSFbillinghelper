# Shared office password staff type at /login.
OCSF_PASSWORD=change-me

# Random secret used to sign the auth cookie (HMAC).
# Generate one with: openssl rand -hex 32
OCSF_COOKIE_SECRET=change-me-to-a-long-random-string
